---
name: PasswordInput
route: /forms/password-input
---

import { useState } from "react";
import { Playground } from "../../Playground.tsx";
import { PasswordInput } from "./PasswordInput.tsx";

# PasswordInput

I am an input field for text, with hidden input.

<Playground>
  {() => {
    const [value, setValue] = useState("Edit Me");
    return <PasswordInput value={value} onChange={setValue} />;
  }}
</Playground>

I can have a placeholder text.

<Playground>
  {() => {
    const [value, setValue] = useState("");
    return (
      <PasswordInput placeholder="Edit Me" value={value} onChange={setValue} />
    );
  }}
</Playground>

I can be disabled.

<Playground>
  {() => {
    const [value, setValue] = useState("");
    return (
      <PasswordInput
        isDisabled
        placeholder="I cannot be edited"
        value={value}
        onChange={setValue}
      />
    );
  }}
</Playground>
